- 2026-03-26：腾讯云服务器 `txy-openclaw` 已重新确认可通过 SSH 直连，登录用户 `ubuntu`，主机名 `VM-0-6-ubuntu`，当前目录 `/home/ubuntu`。
- 2026-03-26：用户确认腾讯云服务器 `root` 环境疑似重装；经检查 `/root/.openclaw/openclaw.json` 不存在，旧 root 侧 OpenClaw 配置已丢失，后续不得再假设 root 下有可迁移配置。
- 2026-03-26：为重建腾讯云 OpenClaw 配置，用户重新提供了关键凭据：OpenRouter 的 `openai/gpt-5.3-chat` 配置、DeepSeek API key、飞书第一套配置（用于腾讯云）、Google AI API key。
- 2026-03-26：飞书两套配置中，用户明确指定“第一个”用于腾讯云服务器上的 OpenClaw；企业微信通道暂时先不配。
- 2026-03-26：用户明确要求：腾讯云 OpenClaw 这次重建完成后，配置文件需双份备份——本地一份、云端一份，使用专门目录存放。
- 2026-03-26：当前主要卡点不是配置内容，而是腾讯云服务器 `ubuntu` 用户的 Node/NVM 非交互环境；`openclaw` 在 SSH 非交互会话里仍报 Node v18.19.1，不满足 Node.js v22.12+ 要求。
- 2026-03-26：由于我多次远程修复 shell 环境脚本写错，用户提出改由其手动处理交互环境问题；我已给出一组用于加载 NVM、切换 Node 22、写入 `.bashrc`/`.profile`、验证 `openclaw status` 的命令，等待用户执行并回传结果。
- 2026-03-26：已约定后续配置备份目录方案：云端 `/home/ubuntu/openclaw-backups/`；本地 `/Users/jingboss/.openclaw/workspace/openclaw-backups/tencent-cloud/`。
- 2026-03-26：用户新增高优先级长期工作原则：凡是我已具备执行条件、风险可控、耗时不长（通常 30 分钟内可完成）的技术操作，应优先由我自己直接完成，不把本应由我承担的执行步骤转交给用户；只有涉及真人登录/验证码/外部不可逆动作/权限确实不足/必须用户主观决策时，才请求用户介入。
