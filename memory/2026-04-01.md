# 2026-04-01

- 用户明确强化默认执行规则：只要用户未明确反对/叫停/改方向，我应默认继续推进当前最合理的下一步，不要反复停在“要不要继续”。
- 用户对当前协作的核心要求进一步明确：嫌慢、不够收口、不够像在干活；后续汇报应尽量压缩为“已完成 / 卡点 / 下一步”，少铺陈。
- 浏览器自动化被用户明确上升为核心能力缺口：若不能自己读页面、点页面、跑网页登录流程，会严重影响价值；需持续推进“浏览器自动化 + RPA”能力建设。
- 用户新增自动化目标：后续希望我能读取飞书消息/飞书链接内容、接入公众号后台、评估 GPT 网页接入，用于朋友圈文案与评论回复质量提升。
- 飞书故障关键定位：当前生效配置 `~/.openclaw/openclaw.json` 中只有 iMessage，`plugins.allow` 仅含 `imessage`，`channels` 仅含 `imessage`，飞书通道未被当前运行配置启用；而 `openclaw.json.save` 中存在可恢复的飞书配置段，说明问题核心是飞书配置被从当前主配置移除，而非飞书后台应用本身失效。
- 用户在飞书开发者后台手动核查结果：应用已启用、事件订阅开启、消息权限存在，因此飞书后台侧基础项正常。
- 当前主配置文件已确认路径为 `~/.openclaw/openclaw.json`；本地存在大量 `openclaw.json.bak*`、`openclaw.json.save` 与 `openclaw.json.clobbered.*` 备份，说明并非完全无备份，而是缺乏清晰的回滚与自动恢复机制。
- `openclaw.json.save` 中可确认的飞书最小配置段包含：`enabled: true`、`connectionMode: websocket`、`domain: feishu`、`groupPolicy: open`、`dmPolicy: pairing`、`defaultAccount: default`、账户 `default` 下含 `appId`、`appSecret`、`botName`；后续应以“将该段恢复到当前主配置 + 在 plugins.allow 中加入 feishu + 重启 gateway + 验证 channels/health”作为最小修复路径。
- 用户设备侧另一个即时问题：Safari 中网页语音输入/麦克风权限被误关。已定位用户曾误入“编辑 → 语音”菜单而非网站权限设置；正确方向是通过地址栏网站设置或 Safari 设置中的网站权限，将对应站点麦克风改为允许。
- 本轮用户明确要求先做“总体盘点 + 下一步排序”，随后确认采用“TODO.md + 首个子代理试运行方案一起做”。已重整 `TODO.md`，并新建 `projects/agent-pilot/first-subagent-pilot.md`，将首个试运行子代理收敛为“内容拆解与初稿子代理”。
- 已完成该子代理首轮真实试跑：题目为《员工主动辞职，还能不能拿赔偿？》；已归档首轮样稿至 `projects/agent-pilot/samples/2026-04-01-离职赔偿短视频口播.md`，并将复盘结论写入 `projects/agent-pilot/reviews/2026-04-01-首轮试跑复盘.md`。当前判断为“保留，继续测试”，下一步应再跑第二个不同场景样本题验证稳定性。
- 用户新增强诉求：公众号发布不要讲太细，目标是“2 小时后发出来”，且用户只负责扫码。已据此将公众号后台自动发布/半自动发布列为当前刚需自动化方向，并设置 2 小时后提醒 cron，提醒届时直接推进扫码与发布动作。
- 今日公众号首篇与朋友圈转发均已实际发出，证明内容生产链路已可执行；用户随后明确要求：后续优先安排公众号后台浏览器自动化研究，不想再手动发。已据此更新 TODO，并新建 `projects/wechat-content-system/docs/browser-automation-research-plan.md` 作为后续研究基线。
- 用户确认采纳“云端虾做主脑、本地虾做执行节点”的协作方向。已在工作区落地 `ROLE_CLOUD.md`、`ROLE_LOCAL.md`、`HANDOFF.md` 三份规则文件，用于后续双虾协作与交接。
- 用户新增明确要求：不要出现“像锁屏一样自己下线”的体感。后续任务未收完时，不得假装后台持续推进；一旦卡住，应尽快主动汇报“已完成 / 卡点 / 下一步”，减少用户反复探活确认。
