# 2026-03-29

- 用户要求安装 ClawHub 相关技能，并先确认 ClawHub 本身是否装好。
- 已成功安装 `steipete/clawdis@clawhub`，安装位置为 `~/.agents/skills/clawhub`，供 OpenClaw 使用。
- 已全局安装 `clawhub` CLI 本体（`npm i -g clawhub`）。
- 已成功安装 `skill-creator` 到工作区：`/Users/jingboss/.openclaw/workspace/skills/skill-creator`；该技能安装时触发 suspicious 告警，用户明确同意后使用 `--force` 安装。
- 多次尝试安装其他 ClawHub 技能时，频繁遭遇 ClawHub 速率限制（Rate limit exceeded），暂未继续批量安装。
- 用户一度要求：剩余技能改为明天再装，并由我自行设置定时任务推进。
- 随后用户叫停腾讯 SkillHub 相关探索，并要求更新任务面；当前不再推进腾讯 SkillHub 方向，剩余 ClawHub 技能自动续装计划也不再视为默认执行事项，待用户后续明确再恢复。
- 已根据当前安装结果整理技能台账文件 `SKILLS_STATUS.md`，记录已装 / 未装 / 高风险 / 已内置能力与优先级建议。
- 用户要求参考 `openclaw-memory` 的思路，改为在本地落一套免费版记忆系统；已在工作区创建 `skills/local-memory-system/` 及 `memory/profiles/`、`memory/projects/`、`memory/index/`、`memory/inbox/` 等骨架，并写入检索地图与模板。
