# 2026-03-25

- 用户要求先暂停子代理落地，优先做基础建设：规则体系、知识库体系、归档体系、复盘与记忆机制。
- 已在工作区新建轻量官网目录：`vercel-law-site/`，包含静态首页骨架、样式、Vercel 配置与 README。
- 官网当前确定的联系信息：邮箱 `61111825@qq.com`；公众号名称暂由我负责命名，先采用临时名称“婧律师职场权益”作为占位方案。
- 用户要求：以后浏览器里如果要去腾讯云服务器部署/安装“小龙虾”，应由我负责想办法打通并尽量自己操作完成。
- 用户确认后续优先走 SSH 直连服务器方案，而不是长期依赖浏览器接管。
- 用户提供新的腾讯云服务器 SSH 公网入口：`43.160.216.195:22`，用户名 `ubuntu`；当前从本地可连到 22 端口，但用现提供密码登录被拒绝，说明网络已通、问题收敛到认证层。
- 今日已完成 Ubuntu 服务器一轮重要修复：识别 root/ubuntu 混装问题；将 OpenClaw 工作目录迁到 `/home/ubuntu/.openclaw`；补齐 Ubuntu 用户 Node 22+、pnpm 与 OpenClaw；修复 `gateway.mode=local`；将 `gateway.bind` 从 loopback 改为 `lan`；当前 `openclaw qr --json` 已能生成 `ws://10.3.0.6:18789` 的局域网接入信息。
- 当前服务器侧结论：OpenClaw 主体与 gateway 已基本修通，剩余问题集中在节点/客户端实际连入，以及 SSH 认证与后续长期可操作入口固化。
- 腾讯云网页终端已确认目标机器为 `ubuntu@VM-0-6-ubuntu:/home/ubuntu`；`openclaw status` 已恢复正常，gateway service 处于 installed/enabled/running 状态，Dashboard 为 `http://10.3.0.6:18789/`。
- 浏览器接管问题的直接根因已定位：Chrome 中的 OpenClaw Browser Relay 插件不是没安装，而是缺少“网关令牌（gateway token）”，因此插件无法认证连接，本机 relay 一直未真正挂给我。
- 用户明确希望以后尽量由我自己进入窗口、自己打字、自己部署，不要长期依赖其手工逐步输入；该事项已作为后续基础设施目标记录。
- 用户确认：对于“小龙虾”、OpenClaw、节点、插件、云端部署微信等近期变化快的技术问题，后续默认先查再答，不凭旧经验直接判断。
- 用户明确后续很多工作将以微信为主要入口/工作界面来设计，云端部署微信需要作为重点研究方向。
- 用户新增长期执行任务：研究并搭建“朋友圈助手”，目标是可提前安排未来 3 天内容，后续作为日常执行机制而非一次性功能。
- 用户新增长期执行任务：抖音/视频号后台消息回复与评论区维护后续也由我负责设计和执行，重点是用一句话高效回复、把有效用户自然导向微信，并筛选有价值评论做维护。
- 用户确认：以上内容一定会涉及知识库搭建，且后续要作为“小虾助理”分工的基础，不是临时话术。
- 用户新增并确认可长期执行的朋友圈规则：统一按保守标准控制在 6 行以内；前三行决定点击，必须分别承担痛点/场景/悬念钩子；发前统一自检“前三行有没有让人想点开”；比行数更关键的是不能写得像广告，要像真实经历/真实判断；后续朋友圈助手与内容库一律按这版规则执行。
- 用户确认：云端那个脑子后续不应再由其从头调教，需把重要规则沉淀为可迁移知识库，并安排“启动必读索引 + 固定启动口令/启动协议”，让云端脑子每次启动先读取规则再进入正式对话；该事项已升级为后续重要主线任务，待给云端配置更聪明脑子后优先安排。
- 用户新增基础人格规则：说谎绝对不允许。后续在估时、汇报、任务状态、已完成/未完成边界、技术判断、是否需要协助等方面必须如实表达，宁可明确说不知道/未完成/已超时，也不能用模糊话术或顺口估时把事实说偏。
- 用户新增执行规则：只要出现卡点，必须及时回报，不准等到原定阶段时间到了才说；卡点一出现，就要立刻说明卡点内容、是否需要用户协助、如果不需要则自己预计多久处理掉。
- 用户新增执行规则：但凡出现需要用户协助的时刻，必须主动提醒，不能等用户追问后才说。
