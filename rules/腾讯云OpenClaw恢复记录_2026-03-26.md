# 腾讯云 OpenClaw 恢复记录（2026-03-26）

## 一、背景

本次处理对象为：**腾讯云服务器上的 OpenClaw**。

已确认：
- 服务器可通过 SSH 直连，别名 `txy-openclaw`
- 登录用户：`ubuntu`
- 主机名：`VM-0-6-ubuntu`
- 当前旧 root 配置迁移路线已失效，因为 `/root/.openclaw/openclaw.json` 不存在，root 侧旧配置已不可依赖

## 二、今日已确认的关键事实

### 1. SSH 管理链路已恢复
- 已验证可从本地直接 SSH 进入腾讯云服务器
- 后续无需再重复向用户确认是否能 SSH 登录该机器

### 2. root 旧配置已丢失
- 经检查，`/root/.openclaw/openclaw.json` 不存在
- 结论：不可再按“从 root 迁移旧配置到 ubuntu”思路继续执行

### 3. 用户重新提供了关键配置来源
已重新拿到以下配置来源：
- OpenRouter：`openai/gpt-5.3-chat`
- DeepSeek API key
- Google AI API key
- 飞书第一套配置（指定用于腾讯云）

### 4. gateway 启动阻断项已定位并修复
问题根因不是权限，而是：
- 配置文件一度损坏，导致 `openclaw status` 无法读取
- 修复配置后，又发现 gateway 启动被阻止：`gateway.mode=local` 未设置
- 现已补上 `gateway.mode=local`

### 5. 当前 OpenClaw 主骨架已恢复
当前已确认：
- `openclaw status` 可正常读取配置
- gateway 已可达
- systemd user service 已 running
- Feishu 通道状态为 `ON / OK / configured`

## 三、当前恢复到的状态

截至本记录写入时，腾讯云上的 OpenClaw 已恢复到以下状态：

### 已恢复
- 配置文件可读取
- Node 22 环境可用
- gateway 服务可启动并可达
- 飞书通道可识别、可用
- 本地到腾讯云的 SSH 管理链路稳定

### 尚未完成
- OpenRouter 完整补回
- DeepSeek 完整补回
- Google AI 默认模型策略补齐
- 配置双备份（云端一份、本地一份）
- 安全项加固（尤其 Feishu `groupPolicy=open`）

## 四、当前风险提示

当前 `openclaw status` 已提示安全问题，主要包括：
- `channels.feishu.groupPolicy = open`
- 在开放群触发下，工具暴露风险偏高
- 目前适合先恢复可用性，不适合误以为安全已经收口

结论：
**当前处于“功能恢复成功，但安全策略尚未收紧”的状态。**

## 五、接下来建议动作

建议按以下顺序继续：
1. 补全 OpenRouter / DeepSeek / Google AI 模型配置
2. 建立配置双备份
   - 云端：`/home/ubuntu/openclaw-backups/`
   - 本地：`/Users/jingboss/.openclaw/workspace/openclaw-backups/tencent-cloud/`
3. 输出一份当前腾讯云配置接管清单
4. 评估是否将 Feishu `groupPolicy` 从 `open` 收紧为 `allowlist`

## 六、一句话结论

**今天不是白折腾：腾讯云上的 OpenClaw 已从“不可用/配置损坏”恢复到“gateway 可达、Feishu 可用、可继续接管补全”的状态。**
