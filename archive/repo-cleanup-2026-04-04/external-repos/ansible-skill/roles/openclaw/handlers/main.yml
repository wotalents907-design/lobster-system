---
# OpenClaw role handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: Restart OpenClaw
  ansible.builtin.systemd:
    name: openclaw
    state: restarted
  when: openclaw_service_enabled | default(false) | bool
